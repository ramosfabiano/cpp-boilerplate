{
  "name": "C++ DevContainer (Debian Bookworm)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {},
  "extensions": [
    "ms-vscode.cpptools"
  ],
  "mounts": [
    "source=${env:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,readonly",
    "source=${env:HOME}/.ssh,target=/home/vscode/.ssh,type=bind,readonly"
  ]
  "features": {},
  "remoteUser": "vscode"
}

